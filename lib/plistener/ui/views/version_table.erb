<table class="table table-striped">
  <% @leaves.each do |key, value| %>
    <tr>
      <td><%= value.class %></td>
      <td class="key">
        <%= key.map {|seg| %{<span class="key-seg">#{ seg }</span>} }.join ':' %>
      </td>
      <td><%= value %></td>
    </tr>
  <% end %>
</table>